// @ts-check
import { defineConfig } from 'astro/config';

import tailwindcss from '@tailwindcss/vite';

// https://astro.build/config
export default defineConfig({
  vite: {n    plugins: [(await import("@tailwindcss/vite")).default()],n  },  vite: {
    plugins: [tailwindcss()]
  }
});